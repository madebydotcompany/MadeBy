import Image from "next/image";
import Link from "next/link";
import { Button } from "@/components/ui/button";
import type { Studio } from "@/features/studios/types";
const Arrow = () => <span aria-hidden="true">↗</span>;
function Heading({ eyebrow, title, href }: { eyebrow: string; title: string; href?: string }) { return <div className="flex items-end justify-between gap-5"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-[var(--studio-accent)]">{eyebrow}</p><h2 className="mt-3 font-display text-4xl sm:text-5xl">{title}</h2></div>{href && <Link href={href} className="text-sm underline underline-offset-4">View all <Arrow /></Link>}</div>; }
export function StudioPage({ studio }: { studio: Studio }) { const collection = studio.collections[0]; const journal = studio.journal[0]; const video = studio.videos[0]; return <main style={{ "--studio-accent": studio.theme.accent, "--studio-wash": studio.theme.wash, "--studio-ink": studio.theme.ink } as React.CSSProperties}><section className="relative isolate min-h-[620px] text-white"><Image src={studio.hero} alt={`${studio.name} work`} fill priority sizes="100vw" className="-z-20 object-cover" /><div className="absolute inset-0 -z-10 bg-gradient-to-t from-black/70 via-black/10 to-black/30" /><div className="mx-auto flex min-h-[620px] max-w-7xl flex-col px-5 py-7 lg:px-8"><div className="flex justify-between"><Link href="/" className="font-display text-2xl">StudioVerse.</Link><Link href="/explore" className="text-sm">← Explore</Link></div><div className="mt-auto"><p className="text-xs uppercase tracking-[.2em] text-white/70">{studio.discipline}</p><h1 className="mt-4 font-display text-6xl leading-[.85] sm:text-8xl">{studio.name}</h1></div></div></section><section className="mx-auto max-w-7xl px-5 lg:px-8"><div className="relative -mt-10 grid gap-6 rounded-[2rem] bg-canvas p-6 shadow-soft lg:grid-cols-[1.3fr_1fr_auto] lg:items-center"><div className="flex items-center gap-4"><Image src={studio.avatar} alt={studio.owner} width={72} height={72} className="rounded-full" /><div><p className="font-display text-3xl">{studio.owner} <span className="text-[var(--studio-accent)]">✓</span></p><p className="text-sm text-muted">{studio.location}</p></div></div><div className="flex gap-5">{studio.stats.map(stat => <div key={stat.label}><p className="font-display text-2xl">{stat.value}</p><p className="text-xs text-muted">{stat.label}</p></div>)}</div><Button asChild size="sm"><Link href="/notifications">Follow</Link></Button></div></section><section className="mx-auto grid max-w-7xl gap-10 px-5 py-24 lg:grid-cols-2 lg:px-8"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-[var(--studio-accent)]">About the studio</p><p className="mt-5 font-display text-4xl leading-tight sm:text-5xl">{studio.bio}</p></div><div className="lg:pt-10"><p className="leading-7 text-muted">A practice built from material attention and a belief that the objects around us can make daily life feel more intentional.</p><div className="mt-7 flex gap-5">{studio.socials.map(item => <Link href={item.href} key={item.label} className="text-sm underline underline-offset-4">{item.label} <Arrow /></Link>)}</div><Button asChild className="mt-8 bg-[var(--studio-accent)]"><Link href="/messages">Request a custom creation</Link></Button></div></section><section className="bg-[var(--studio-wash)] py-24"><div className="mx-auto max-w-7xl px-5 lg:px-8"><Heading eyebrow="Selected work" title="Portfolio" /><div className="mt-9 grid grid-cols-2 gap-4 md:grid-cols-4">{studio.portfolio.map((item, index) => <div key={item.title} className={index === 1 ? "col-span-2" : ""}><div className={`relative overflow-hidden rounded-3xl ${item.size === "tall" ? "aspect-[.72]" : item.size === "wide" ? "aspect-[1.6]" : "aspect-square"}`}><Image src={item.image} alt={item.title} fill className="object-cover" /></div><p className="mt-2 text-sm italic text-muted">{item.title}</p></div>)}</div></div></section><section className="mx-auto max-w-7xl px-5 py-24 lg:px-8"><Heading eyebrow="Available now" title="Objects to live with" href="/explore" /><div className="mt-9 grid gap-6 sm:grid-cols-3">{studio.products.map(product => <Link href={`/products/${product.slug}`} key={product.slug} className="group"><div className="relative aspect-[.85] overflow-hidden rounded-3xl"><Image src={product.image} alt={product.name} fill className="object-cover transition group-hover:scale-105" /></div><div className="mt-3 flex justify-between"><h3 className="font-display text-2xl">{product.name}</h3><span className="text-sm text-muted">{product.price}</span></div></Link>)}</div></section><section className="mx-auto max-w-7xl px-5 pb-24 lg:px-8"><Link href={`/collections/${collection.slug}`} className="relative block min-h-[420px] overflow-hidden rounded-[2rem] bg-[var(--studio-ink)] p-8 text-white sm:p-12"><Image src={collection.image} alt={collection.name} fill className="object-cover opacity-40" /><div className="relative mt-48"><p className="text-xs uppercase tracking-[.18em]">Collection</p><h2 className="mt-3 font-display text-6xl">{collection.name}</h2><p className="mt-3">{collection.note}</p><p className="mt-6 text-sm underline">Enter collection <Arrow /></p></div></Link></section><section className="border-y border-line py-24"><div className="mx-auto grid max-w-7xl gap-10 px-5 lg:grid-cols-2 lg:px-8"><div><Heading eyebrow="Studio Journal" title="From the worktable" /><Link href={`/journal/${journal.slug}`} className="mt-8 grid gap-5 sm:grid-cols-2"><div className="relative aspect-square overflow-hidden rounded-3xl"><Image src={journal.image} alt={journal.title} fill className="object-cover" /></div><div className="self-center"><p className="text-xs text-muted">{journal.date}</p><h3 className="mt-3 font-display text-3xl">{journal.title}</h3><p className="mt-4 text-sm underline">Read entry <Arrow /></p></div></Link></div><div><Heading eyebrow="Moving image" title="Videos" /><Link href={`/reels/${video.slug}`} className="relative mt-8 block aspect-video overflow-hidden rounded-3xl"><Image src={video.image} alt={video.title} fill className="object-cover" /><div className="absolute inset-0 grid place-items-center bg-black/25 text-4xl text-white">▶</div><p className="absolute bottom-5 left-5 font-display text-2xl text-white">{video.title}</p></Link></div></div></section><section className="mx-auto max-w-7xl px-5 py-20 lg:px-8"><div className="grid gap-8 border-b border-line pb-16 md:grid-cols-2"><div><p className="text-xs uppercase tracking-[.18em] text-[var(--studio-accent)]">Next gathering</p><h2 className="mt-4 font-display text-4xl">{studio.event.title}</h2><p className="mt-4 text-muted">{studio.event.date}<br />{studio.event.place}</p><Link href="/messages" className="mt-6 inline-block text-sm underline">RSVP to attend <Arrow /></Link></div><div><p className="text-xs uppercase tracking-[.18em] text-[var(--studio-accent)]">Stay in the orbit</p><p className="mt-4 font-display text-4xl">Follow along, or make something together.</p><div className="mt-7 flex gap-3"><Button asChild><Link href="/notifications">Follow {studio.name}</Link></Button><Button asChild variant="outline"><Link href="/messages">Contact the studio</Link></Button></div></div></div></section></main>; }
