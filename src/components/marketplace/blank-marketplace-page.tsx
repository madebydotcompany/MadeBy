"use client";
import Image from "next/image";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { blankProducts } from "@/features/community/data";
import { useCart } from "@/features/cart/cart-provider";
import { useMock } from "@/features/commerce/mock-provider";
export function BlankMarketplacePage() { const [category, setCategory] = useState("All"); const [selected, setSelected] = useState<(typeof blankProducts)[number] | null>(null); const { addLine } = useCart(); const { setNotice } = useMock(); const categories = ["All", "Apparel", "Footwear", "Home", "Wall art", "Stationery"]; const visible = category === "All" ? blankProducts : blankProducts.filter(item => item.type === category); const add = (item: (typeof blankProducts)[number]) => { addLine({ id: `blank-${item.name}`, productSlug: `blank-${item.name.toLowerCase().replaceAll(" ", "-")}`, name: item.name, unitPrice: Number(item.price.replace(/[^0-9]/g, "")), quantity: 1, image: item.image }); setNotice(`${item.name} added to cart`); }; return <main className="mx-auto max-w-7xl px-5 py-14 lg:px-8"><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">Blank product marketplace</p><div className="mt-4 flex flex-col justify-between gap-6 sm:flex-row sm:items-end"><div><h1 className="max-w-3xl font-display text-5xl leading-none sm:text-6xl">Start with a beautiful blank.</h1><p className="mt-5 max-w-xl text-muted">Source the foundation for your next collection, then add your own point of view.</p></div><Button onClick={() => setNotice("Your design drafts are ready to start")}>View design drafts</Button></div><div className="mt-10 flex gap-2 overflow-auto pb-2">{categories.map(item => <button key={item} onClick={() => setCategory(item)} className={`whitespace-nowrap rounded-full px-4 py-2 text-sm ${category === item ? "bg-ink text-canvas" : "border border-line hover:bg-ink/5"}`}>{item}</button>)}</div><div className="mt-9 grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-4">{visible.map(item => <article className="group" key={item.name}><button onClick={() => setSelected(item)} className="relative block aspect-[.85] w-full overflow-hidden rounded-3xl bg-ink/5"><Image src={item.image} alt={item.name} fill sizes="(max-width: 640px) 50vw, 25vw" className="object-cover transition duration-700 group-hover:scale-105" /><span className="absolute left-3 top-3 rounded-full bg-canvas/90 px-2.5 py-1 text-[10px] backdrop-blur">{item.type}</span></button><div className="mt-3"><h2 className="font-display text-2xl">{item.name}</h2><p className="mt-1 text-sm text-muted">{item.price}</p><Button onClick={() => add(item)} size="sm" variant="outline" className="mt-3">Add to cart</Button></div></article>)}</div>{selected && <div className="fixed inset-0 z-50 grid place-items-center bg-black/45 p-5"><section className="w-full max-w-md rounded-3xl bg-canvas p-6 shadow-soft"><button onClick={() => setSelected(null)} className="float-right text-sm">Close</button><h2 className="font-display text-4xl">{selected.name}</h2><p className="mt-4 text-sm leading-6 text-muted">A quality blank selected for studio experimentation. Choose it now, then create a design when you’re ready.</p><p className="mt-5 font-medium">{selected.price}</p><Button className="mt-6" onClick={() => { add(selected); setSelected(null); }}>Add blank to cart</Button></section></div>}</main>; }
