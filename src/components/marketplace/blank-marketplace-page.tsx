"use client";
import Image from "next/image";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { blankProducts } from "@/features/community/data";
export function BlankMarketplacePage() { const [category, setCategory] = useState("All"); const categories = ["All", "Apparel", "Footwear", "Home", "Wall art", "Stationery"]; const visible = category === "All" ? blankProducts : blankProducts.filter(item => item.type === category); return <main className="mx-auto max-w-7xl px-5 py-14 lg:px-8"><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">Blank product marketplace</p><div className="mt-4 flex flex-col justify-between gap-6 sm:flex-row sm:items-end"><div><h1 className="max-w-3xl font-display text-5xl leading-none sm:text-6xl">Start with a beautiful blank.</h1><p className="mt-5 max-w-xl text-muted">Choose a product designed for creative expression, add your artwork, and offer it through your Studio when print-on-demand is connected.</p></div><Button>View design drafts</Button></div><div className="mt-10 flex gap-2 overflow-auto pb-2">{categories.map(item => <button key={item} onClick={() => setCategory(item)} className={`whitespace-nowrap rounded-full px-4 py-2 text-sm ${category === item ? "bg-ink text-canvas" : "border border-line hover:bg-ink/5"}`}>{item}</button>)}</div><div className="mt-9 grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-4">{visible.map(item => <article className="group" key={item.name}><div className="relative aspect-[.85] overflow-hidden rounded-3xl bg-ink/5"><Image src={item.image} alt={item.name} fill sizes="(max-width: 640px) 50vw, 25vw" className="object-cover transition duration-700 group-hover:scale-105" /><span className="absolute left-3 top-3 rounded-full bg-canvas/90 px-2.5 py-1 text-[10px] backdrop-blur">{item.type}</span></div><div className="mt-3"><h2 className="font-display text-2xl">{item.name}</h2><p className="mt-1 text-sm text-muted">{item.price}</p><Button variant="quiet" size="sm" className="mt-2 px-0">Start a design ↗</Button></div></article>)}</div></main>; }
