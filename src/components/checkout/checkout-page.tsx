"use client";
import Link from "next/link";
import { Button } from "@/components/ui/button";
import { CheckoutDetailsForm, OrderSummary } from "@/components/checkout/checkout-components";
import { useCart } from "@/features/cart/cart-provider";
import type { CartLine } from "@/features/products/types";
export function CheckoutPage({ fallbackLine }: { fallbackLine: CartLine }) { const { lines, hydrated } = useCart(); const orderLines = lines.length ? lines : [fallbackLine]; if (!hydrated) return <main className="mx-auto max-w-5xl px-5 py-20">Preparing checkout…</main>; return <main className="mx-auto max-w-5xl px-5 py-10 lg:px-8"><Link href={lines.length ? "/cart" : `/products/${fallbackLine.productSlug}`} className="text-sm text-muted hover:text-ink">← Return to {lines.length ? "cart" : "work"}</Link><div className="mt-10"><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">Checkout</p><h1 className="mt-3 font-display text-5xl">A few final details.</h1></div><div className="mt-10 grid gap-8 lg:grid-cols-[1.1fr_.9fr]"><div><CheckoutDetailsForm /><section className="mt-6 rounded-3xl border border-line p-6"><p className="font-display text-2xl">Shipping method</p><label className="mt-5 flex cursor-pointer items-center justify-between rounded-2xl border border-ink bg-ink/[.03] p-4 text-sm"><span><input type="radio" defaultChecked name="shipping" className="mr-3 accent-accent" />Best available shipping</span><span>Calculated after address</span></label></section><Button className="mt-6 w-full" size="lg" disabled>Payment setup required</Button><p className="mt-3 text-center text-xs leading-5 text-muted">Payment is intentionally disabled until StudioVerse has a verified Stripe payment and order-fulfilment integration.</p></div><OrderSummary lines={orderLines} /></div></main>; }
