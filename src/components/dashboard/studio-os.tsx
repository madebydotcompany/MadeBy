"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { aiTools, dashboardSections, recentOrders, revenueData, weeklyVisits } from "@/features/dashboard/data";

function Icon({ name }: { name: string }) { const map: Record<string, string> = { Overview: "⌘", Revenue: "↗", Products: "□", Collections: "◫", Orders: "▤", Inventory: "◈", Followers: "◎", Messages: "◌", "Studio Journal": "✦", Videos: "▷", Reviews: "★", "Customization Requests": "✎", "Commission Requests": "✧", Analytics: "⌁", Notifications: "◔", Settings: "⚙" }; return <span className="w-5 text-center text-sm" aria-hidden="true">{map[name]}</span>; }
function Card({ children, className = "" }: { children: React.ReactNode; className?: string }) { return <section className={`rounded-3xl border border-line bg-canvas p-5 shadow-card sm:p-6 ${className}`}>{children}</section>; }
function MiniChart({ values, color = "#bd5335" }: { values: number[]; color?: string }) { const max = Math.max(...values); const points = values.map((value, i) => `${i * (100 / (values.length - 1))},${88 - (value / max) * 72}`).join(" "); return <svg viewBox="0 0 100 100" preserveAspectRatio="none" className="h-40 w-full overflow-visible" role="img" aria-label="Revenue trend chart"><defs><linearGradient id="chart-fill" x1="0" y1="0" x2="0" y2="1"><stop stopColor={color} stopOpacity=".22" /><stop offset="1" stopColor={color} stopOpacity="0" /></linearGradient></defs><path d={`M0,100 L${points} L100,100 Z`} fill="url(#chart-fill)" /><polyline fill="none" stroke={color} strokeWidth="2.5" vectorEffect="non-scaling-stroke" points={points} /><line x1="0" x2="100" y1="88" y2="88" stroke="currentColor" className="text-line" vectorEffect="non-scaling-stroke" /></svg>; }
function EmptyWorkspace({ section }: { section: string }) { const prompts: Record<string, { title: string; note: string; action: string }> = { Messages: { title: "A quiet inbox is a lovely thing.", note: "Conversations with collectors and collaborators will gather here.", action: "Set message preferences" }, "Customization Requests": { title: "No customisations waiting.", note: "When a collector asks to make a work their own, you can review every detail here.", action: "View made-on-demand works" }, "Commission Requests": { title: "Your next brief will arrive here.", note: "Keep your availability current so the right collaborations can find you.", action: "Update availability" }, Notifications: { title: "Nothing new to interrupt your practice.", note: "Orders, comments, and Studio activity will appear here when there is something worth knowing.", action: "Notification settings" } }; const item = prompts[section] ?? { title: `${section}, with room to grow.`, note: `This is the home for your Studio's ${section.toLowerCase()}. Add your first item when you're ready.`, action: `Create ${section === "Studio Journal" ? "an entry" : "new item"}` }; return <Card className="grid min-h-[380px] place-items-center text-center"><div className="max-w-sm"><div className="mx-auto grid h-14 w-14 place-items-center rounded-full bg-accent/10 font-display text-2xl text-accent">✦</div><h2 className="mt-6 font-display text-4xl">{item.title}</h2><p className="mt-4 text-sm leading-6 text-muted">{item.note}</p><Button variant="outline" className="mt-7">{item.action}</Button></div></Card>; }
function Overview() { return <><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">Mira Kato Studio</p><h1 className="mt-3 font-display text-5xl leading-none">Good morning, Mira.</h1><p className="mt-3 text-sm text-muted">Here’s the shape of your studio today.</p></div><Button>+ Add new work</Button></div><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Revenue this month" value="$4,860" trend="↑ 18.2% from June" /><Metric label="New collectors" value="38" trend="↑ 12 this month" /><Metric label="Works in progress" value="07" trend="3 need your attention" warm /><Metric label="Studio visits" value="2,419" trend="↑ 24.6% from June" /></div><div className="mt-5 grid gap-5 xl:grid-cols-[1.5fr_.8fr]"><Card><div className="flex items-start justify-between"><div><p className="text-sm font-medium">Revenue</p><p className="mt-2 font-display text-4xl">$31,330</p></div><span className="rounded-full bg-accent/10 px-3 py-1 text-xs text-accent">Last 12 months</span></div><MiniChart values={revenueData} /><div className="flex justify-between text-[11px] text-muted"><span>Aug</span><span>Oct</span><span>Dec</span><span>Feb</span><span>Apr</span><span>Jul</span></div></Card><Card><p className="text-sm font-medium">Your week in the Studio</p><div className="mt-7 flex h-36 items-end justify-between gap-2">{weeklyVisits.map((value, index) => <div className="flex flex-1 flex-col items-center gap-2" key={index}><div className="w-full rounded-t-md bg-accent/80" style={{ height: `${value}%` }} /><span className="text-[10px] text-muted">{["M", "T", "W", "T", "F", "S", "S"][index]}</span></div>)}</div><p className="mt-4 text-sm text-muted"><span className="font-medium text-ink">535 visits</span> this week</p></Card></div><div className="mt-5 grid gap-5 xl:grid-cols-[1.2fr_.8fr]"><Card><div className="flex items-center justify-between"><p className="font-display text-2xl">Recent orders</p><button className="text-sm underline underline-offset-4">View all</button></div><div className="mt-5 divide-y divide-line">{recentOrders.map(order => <div key={order.customer} className="grid grid-cols-[1fr_auto] gap-3 py-4 sm:grid-cols-[1fr_1fr_auto_auto]"><div><p className="text-sm font-medium">{order.customer}</p><p className="mt-1 text-xs text-muted">{order.work}</p></div><span className="hidden text-sm text-muted sm:block">{order.status}</span><span className="rounded-full bg-ink/[.05] px-2.5 py-1 text-xs">{order.status}</span><span className="text-sm">{order.value}</span></div>)}</div></Card><Card className="bg-[#2e4039] text-[#f8f4eb]"><p className="text-xs font-semibold uppercase tracking-[.18em] text-[#e6c994]">Studio pulse</p><p className="mt-4 font-display text-3xl leading-tight">Your Low moon bowl is getting noticed.</p><p className="mt-4 text-sm leading-6 text-white/65">It was saved 47 times this week—more than any other work in your studio.</p><button className="mt-6 text-sm underline underline-offset-4">See product insights ↗</button></Card></div><AITools /></>; }
function Metric({ label, value, trend, warm }: { label: string; value: string; trend: string; warm?: boolean }) { return <Card className={warm ? "bg-[#f1e3d5]" : ""}><p className="text-xs text-muted">{label}</p><p className="mt-3 font-display text-4xl">{value}</p><p className="mt-3 text-xs text-muted">{trend}</p></Card>; }
function AITools() { return <section className="mt-8"><div className="flex items-end justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">Studio intelligence</p><h2 className="mt-3 font-display text-3xl">A little help when you want it.</h2></div><span className="rounded-full border border-line px-3 py-1.5 text-xs text-muted">Coming soon</span></div><div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-5">{aiTools.map(tool => <Card key={tool.name} className="flex min-h-44 flex-col"><span className="text-xl text-accent">✦</span><h3 className="mt-5 font-display text-xl">{tool.name}</h3><p className="mt-2 text-xs leading-5 text-muted">{tool.note}</p><button className="mt-auto pt-5 text-left text-xs underline underline-offset-4">Preview tool</button></Card>)}</div></section>; }
export function StudioOS() { const [active, setActive] = useState("Overview"); const groups = [...new Set(dashboardSections.map(item => item.group))]; return <main className="min-h-screen bg-[#f7f5ef] text-ink"><div className="mx-auto flex max-w-[1600px]"><aside className="sticky top-0 hidden h-screen w-64 shrink-0 border-r border-line bg-canvas px-4 py-7 lg:block"><div className="px-3"><p className="font-display text-2xl">Studio OS<span className="text-accent">.</span></p><p className="mt-1 text-xs text-muted">Mira Kato Studio</p></div><nav className="mt-9 space-y-6" aria-label="Studio OS navigation">{groups.map(group => <div key={group}><p className="px-3 text-[10px] font-semibold uppercase tracking-[.16em] text-muted">{group}</p><div className="mt-2 space-y-1">{dashboardSections.filter(item => item.group === group).map(item => <button key={item.label} onClick={() => setActive(item.label)} className={`flex w-full items-center gap-2 rounded-xl px-3 py-2 text-left text-sm transition ${active === item.label ? "bg-ink text-canvas" : "text-muted hover:bg-ink/[.05] hover:text-ink"}`}><Icon name={item.label} />{item.label}{item.label === "Customization Requests" && <span className="ml-auto rounded-full bg-accent px-1.5 py-.5 text-[10px] text-white">3</span>}</button>)}</div></div>)}</nav></aside><div className="min-w-0 flex-1"><header className="sticky top-0 z-10 flex h-18 items-center justify-between border-b border-line bg-[#f7f5ef]/90 px-5 backdrop-blur lg:h-20 lg:px-10"><div className="lg:hidden"><p className="font-display text-xl">Studio OS<span className="text-accent">.</span></p></div><p className="hidden text-sm text-muted lg:block">Wednesday, 16 July</p><div className="flex items-center gap-3"><button className="rounded-full border border-line px-3 py-2 text-sm">⌕ <span className="hidden sm:inline">Search</span></button><button className="grid h-9 w-9 place-items-center rounded-full border border-line text-sm">◔</button><span className="grid h-9 w-9 place-items-center rounded-full bg-[#bd5335] text-sm text-white">MK</span></div></header><div className="px-5 py-9 lg:px-10 lg:py-12">{active === "Overview" ? <Overview /> : <><div className="mb-8"><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">Studio OS</p><h1 className="mt-3 font-display text-5xl">{active}</h1></div><EmptyWorkspace section={active} /></>}</div></div></div></main>; }
