"use client";
import Image from "next/image";
import { motion } from "framer-motion";

type Studio = { name: string; discipline: string; place: string; image: string; note: string };
export function StudioCard({ studio, index = 0 }: { studio: Studio; index?: number }) { return <motion.article initial={{ opacity: 0, y: 18 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: .2 }} transition={{ duration: .5, delay: index * .06 }} className="group cursor-pointer"><div className="relative aspect-[.82] overflow-hidden rounded-3xl bg-ink/5"><Image src={studio.image} alt={`${studio.name}'s work`} fill sizes="(max-width: 768px) 90vw, 33vw" className="object-cover transition duration-700 group-hover:scale-105" /><span className="absolute left-4 top-4 rounded-full bg-canvas/85 px-3 py-1.5 text-[11px] font-medium backdrop-blur">Studio</span></div><div className="mt-4"><div className="flex items-baseline justify-between gap-3"><h3 className="font-display text-2xl">{studio.name}</h3><span className="text-xs text-muted">{studio.place}</span></div><p className="mt-1 text-sm text-muted">{studio.discipline}</p><p className="mt-3 text-sm italic text-ink/75">“{studio.note}”</p></div></motion.article>; }
