"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
const items = ["Overview", "Artist verification", "Artist approval", "Featured Studios", "Product moderation", "Copyright reports", "Platform analytics", "Commission settings", "User management"];
const queue = [{ name: "Saanvi Studio", detail: "Artist verification · 2 documents", status: "Review" }, { name: "Found Objects, 03", detail: "Product moderation · flagged image", status: "Review" }, { name: "Copyright report #104", detail: "Claim against Stillroom Studio", status: "Investigate" }];
export function AdminPanel() { const [active, setActive] = useState("Overview"); return <main className="min-h-screen bg-[#eff1ed]"><div className="mx-auto flex max-w-[1500px]"><aside className="hidden min-h-screen w-64 border-r border-line bg-canvas p-5 lg:block"><p className="font-display text-2xl">StudioVerse <span className="text-accent">Admin.</span></p><nav className="mt-9 space-y-1">{items.map(item => <button onClick={() => setActive(item)} key={item} className={`w-full rounded-xl px-3 py-2.5 text-left text-sm ${active === item ? "bg-ink text-canvas" : "text-muted hover:bg-ink/5 hover:text-ink"}`}>{item}</button>)}</nav></aside><section className="min-w-0 flex-1 p-5 sm:p-10"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">Platform control</p><h1 className="mt-3 font-display text-5xl">{active}</h1></div><span className="rounded-full bg-ink px-3 py-2 text-xs text-canvas">Admin access</span></div>{active === "Overview" ? <><div className="mt-9 grid gap-4 sm:grid-cols-3"><AdminMetric label="Pending approvals" value="14" note="6 added today" /><AdminMetric label="Reports open" value="03" note="All within target" /><AdminMetric label="Active studios" value="1,284" note="↑ 8.4% this month" /></div><section className="mt-5 rounded-3xl border border-line bg-canvas p-6"><div className="flex items-center justify-between"><h2 className="font-display text-3xl">Needs attention</h2><button className="text-sm underline underline-offset-4">View all</button></div><div className="mt-5 divide-y divide-line">{queue.map(item => <div className="flex flex-wrap items-center justify-between gap-4 py-4" key={item.name}><div><p className="font-medium">{item.name}</p><p className="mt-1 text-sm text-muted">{item.detail}</p></div><Button size="sm" variant="outline">{item.status}</Button></div>)}</div></section></> : <section className="mt-9 grid min-h-[420px] place-items-center rounded-3xl border border-line bg-canvas text-center"><div className="max-w-md"><span className="text-3xl text-accent">✦</span><h2 className="mt-5 font-display text-4xl">{active} workspace</h2><p className="mt-3 text-sm leading-6 text-muted">This modular queue is ready to connect to the matching moderation and platform records in Prisma.</p><Button className="mt-6">Open queue</Button></div></section>}</section></div></main>; }
function AdminMetric({ label, value, note }: { label: string; value: string; note: string }) { return <div className="rounded-3xl border border-line bg-canvas p-5"><p className="text-xs text-muted">{label}</p><p className="mt-3 font-display text-4xl">{value}</p><p className="mt-2 text-xs text-muted">{note}</p></div>; }
