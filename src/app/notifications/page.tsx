"use client";
import { useState } from "react";
const initial = [{ title: "Mira Kato published a new journal entry", note: "Notes from a quieter kiln", time: "12 min ago" }, { title: "Your saved work is nearly gone", note: "Low moon bowl has 2 places left in this firing.", time: "2 hr ago" }, { title: "Saanvi Studio is now on StudioVerse", note: "A new world of resin and textile has opened.", time: "Yesterday" }];
export default function NotificationsPage() { const [items, setItems] = useState(initial); return <main className="mx-auto max-w-3xl px-5 py-14"><div className="flex items-end justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">Your orbit</p><h1 className="mt-3 font-display text-5xl">Notifications</h1></div><button onClick={() => setItems([])} className="text-sm underline">Mark all read</button></div><div className="mt-10 space-y-3">{items.length ? items.map(item => <article className="rounded-2xl border border-line p-5" key={item.title}><p className="font-medium">{item.title}</p><p className="mt-2 text-sm text-muted">{item.note}</p><p className="mt-3 text-xs text-accent">{item.time}</p></article>) : <div className="rounded-3xl border border-dashed border-line p-14 text-center"><p className="font-display text-3xl">You’re all caught up.</p><p className="mt-3 text-muted">New Studio moments will appear here.</p></div>}</div></main>; }
