"use client";
import Link from "next/link";
import { Button } from "@/components/ui/button";
import { useMock } from "@/features/commerce/mock-provider";
import { formatPrice } from "@/features/products/data";
export default function OrdersPage() { const { orders } = useMock(); return <main className="mx-auto max-w-4xl px-5 py-14 lg:px-8"><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">Your collection</p><h1 className="mt-3 font-display text-5xl">Orders</h1>{orders.length ? <div className="mt-10 space-y-4">{orders.map(order => <article key={order.id} className="rounded-3xl border border-line p-6"><div className="flex flex-wrap justify-between gap-4"><div><p className="font-display text-2xl">Order {order.id}</p><p className="mt-1 text-sm text-muted">Placed {order.date} · {order.lines.length} work{order.lines.length !== 1 ? "s" : ""}</p></div><span className="rounded-full bg-accent/10 px-3 py-1.5 text-sm text-accent">{order.status}</span></div><div className="mt-5 border-t border-line pt-4 text-sm"><p>{order.tracking}</p><p className="mt-2 font-medium">Total {formatPrice(order.total)}</p></div></article>)}</div> : <div className="mt-10 rounded-3xl border border-dashed border-line p-12 text-center"><h2 className="font-display text-3xl">No orders yet.</h2><p className="mt-3 text-muted">When you collect a work, its story continues here.</p><Button asChild className="mt-6"><Link href="/explore">Discover Studios</Link></Button></div>}</main>; }
