import { Navigation } from "@/components/layout/navigation";
import { Footer } from "@/components/layout/footer";
import { StudioCard } from "@/components/discovery/studio-card";
import { categories, studios } from "@/features/discovery/data";

export default function ExplorePage() { return <><Navigation /><main className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">Discovery, without the noise</p><h1 className="mt-4 max-w-4xl font-display text-5xl leading-none sm:text-7xl">Follow your curiosity.</h1><p className="mt-6 max-w-xl text-lg leading-8 text-muted">A living edit of artists, objects, practices, and places. The Studio is always the starting point.</p><div className="mt-12 flex gap-3 overflow-x-auto pb-2">{["For you", "Trending", "New studios", "Near you", "Limited drops"].map((label, i) => <button key={label} className={`whitespace-nowrap rounded-full px-5 py-2.5 text-sm ${i === 0 ? "bg-ink text-canvas" : "border border-line hover:bg-ink/5"}`}>{label}</button>)}</div><section className="mt-16"><div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{studios.map((studio, index) => <StudioCard studio={studio} index={index} key={studio.name} />)}</div></section><section className="mt-24 border-t border-line pt-16"><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">Browse by material & practice</p><div className="mt-8 grid grid-cols-2 gap-3 sm:grid-cols-5">{categories.map(category => <div className="rounded-2xl border border-line p-4 text-sm transition hover:-translate-y-1 hover:shadow-card" key={category.name}>{category.name}</div>)}</div></section></main><Footer /></>; }
