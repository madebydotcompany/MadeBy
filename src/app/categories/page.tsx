import Image from "next/image";
import Link from "next/link";
import { Navigation } from "@/components/layout/navigation";
import { Footer } from "@/components/layout/footer";
import { categories } from "@/features/discovery/data";

export default function CategoriesPage() { return <><Navigation /><main className="mx-auto max-w-7xl px-5 py-16 lg:px-8"><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">The index</p><h1 className="mt-4 font-display text-5xl sm:text-7xl">Find your corner of the creative world.</h1><p className="mt-6 max-w-xl text-lg leading-8 text-muted">Every category opens into artists, stories, collections, and objects with a distinctive point of view.</p><div className="mt-16 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{categories.map((category, index) => <Link href="/explore" key={category.name} className={`group relative min-h-72 overflow-hidden rounded-3xl ${index % 5 === 0 ? "sm:col-span-2" : ""}`}><Image alt={category.name} src={category.image} fill sizes="(max-width: 640px) 90vw, 45vw" className="object-cover transition duration-700 group-hover:scale-105" /><div className="absolute inset-0 bg-gradient-to-t from-black/60 via-black/5 to-transparent" /><div className="absolute inset-x-6 bottom-6 flex items-end justify-between text-white"><h2 className="font-display text-4xl">{category.name}</h2><span className="grid h-10 w-10 place-items-center rounded-full border border-white/35 bg-white/10 backdrop-blur">↗</span></div></Link>)}</div></main><Footer /></>; }
