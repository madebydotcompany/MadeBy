import Link from "next/link";
import { notFound } from "next/navigation";
import { Navigation } from "@/components/layout/navigation";
import { Footer } from "@/components/layout/footer";
import { stories } from "@/features/discovery/data";
const toSlug = (value: string) => value.toLowerCase().replaceAll(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "");
export default function JournalPage({ params }: { params: { slug: string } }) { const story = stories.find(item => toSlug(item.title) === params.slug); if (!story) notFound(); return <><Navigation /><main className="mx-auto max-w-3xl px-5 py-14"><nav aria-label="Breadcrumb" className="text-sm text-muted"><Link href="/" className="hover:text-ink">Home</Link><span className="px-2">/</span><Link href="/explore" className="hover:text-ink">Stories</Link><span className="px-2">/</span>{story.title}</nav><p className="mt-12 text-xs font-semibold uppercase tracking-[.18em] text-accent">Studio Journal</p><h1 className="mt-4 font-display text-5xl leading-none sm:text-6xl">{story.title}</h1><p className="mt-6 text-sm text-muted">By {story.artist} · {story.read} · Process, studio life, material</p><article className="mt-12 space-y-7 text-lg leading-8 text-ink/85"><p>There is a particular kind of attention that arrives before a day in the studio begins. It is not efficiency, and it is not certainty. It is simply a willingness to notice what the material has been saying all along.</p><p>I keep returning to the same question: what would make this object worth living with? The answer rarely comes from adding more. More often, it appears in a softened edge, a patient repair, or a colour that changes when the afternoon moves across it.</p><p>This is the work beneath the work: the quiet experiments, the failed tests, the small decisions that become a point of view. I share them because an object is never only an object. It carries the time, care, and curiosity that brought it into being.</p></article></main><Footer /></>; }
