"use client";
import { useState } from "react";
import { Button } from "@/components/ui/button";
export default function CustomOrderPage() { const [sent, setSent] = useState(false); if (sent) return <main className="grid min-h-[70vh] place-items-center p-6 text-center"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">Request sent</p><h1 className="mt-3 font-display text-5xl">A new collaboration begins.</h1><p className="mt-4 max-w-md text-muted">Mira will review your brief inside Studio OS and reply through Messages.</p><Button className="mt-7" onClick={() => setSent(false)}>Create another request</Button></div></main>; return <main className="mx-auto max-w-3xl px-5 py-14 lg:px-8"><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">Made for you</p><h1 className="mt-3 font-display text-5xl">Describe what you imagine.</h1><p className="mt-4 text-muted">Share the details, then a Studio can shape a piece around your life.</p><form onSubmit={event => { event.preventDefault(); setSent(true); }} className="mt-10 grid gap-5 rounded-3xl border border-line p-6"><textarea required placeholder="Describe the work, story, or space you have in mind" className="min-h-32 rounded-xl border border-line bg-transparent p-3 text-sm" /><div className="grid gap-5 sm:grid-cols-2"><input required placeholder="Budget (e.g. $350)" className="h-11 rounded-xl border border-line bg-transparent px-3 text-sm" /><input required type="date" className="h-11 rounded-xl border border-line bg-transparent px-3 text-sm" /></div><input placeholder="Preferred colours, materials, size, or notes" className="h-11 rounded-xl border border-line bg-transparent px-3 text-sm" /><label className="rounded-xl border border-dashed border-line p-5 text-sm text-muted">Reference images placeholder <input type="file" className="mt-2 block text-xs" /></label><Button type="submit">Send request to Studio</Button></form></main>; }
