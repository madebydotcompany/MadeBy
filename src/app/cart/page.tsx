"use client";
import Link from "next/link";
import { Button } from "@/components/ui/button";
import { OrderSummary } from "@/components/checkout/checkout-components";
import { useCart } from "@/features/cart/cart-provider";
export default function CartPage() { const { lines, hydrated, removeLine } = useCart(); if (!hydrated) return <main className="mx-auto max-w-5xl px-5 py-20">Loading your cart…</main>; if (!lines.length) return <main className="mx-auto grid min-h-[60vh] max-w-5xl place-items-center px-5 text-center"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">Your cart</p><h1 className="mt-4 font-display text-5xl">A little room for a future find.</h1><p className="mt-4 text-muted">The works you choose to collect will appear here.</p><Button asChild className="mt-8"><Link href="/explore">Explore Studios</Link></Button></div></main>; return <main className="mx-auto max-w-5xl px-5 py-12 lg:px-8"><Link href="/explore" className="text-sm text-muted hover:text-ink">← Continue discovering</Link><h1 className="mt-7 font-display text-5xl">Your collected works</h1><div className="mt-10 grid gap-8 lg:grid-cols-[1.1fr_.9fr]"><div className="space-y-4">{lines.map(line => <div className="flex items-center justify-between rounded-2xl border border-line p-4" key={line.id}><div><p className="font-display text-xl">{line.name}</p><p className="mt-1 text-sm text-muted">Quantity {line.quantity}</p></div><button onClick={() => removeLine(line.id)} className="text-sm underline underline-offset-4">Remove</button></div>)}</div><div><OrderSummary lines={lines} /><Button asChild className="mt-4 w-full"><Link href="/checkout">Continue to checkout</Link></Button></div></div></main>; }
