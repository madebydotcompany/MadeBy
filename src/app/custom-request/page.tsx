"use client";
import { useState } from "react";
import Link from "next/link";
import { Button } from "@/components/ui/button";
import { useCart } from "@/features/cart/cart-provider";
export default function CustomRequestPage() { const { submitRequest } = useCart(); const [sent, setSent] = useState(false); const [form, setForm] = useState({ description: "", budget: "", deadline: "", notes: "" }); if (sent) return <main className="grid min-h-[70vh] place-items-center p-5 text-center"><div><p className="text-xs uppercase tracking-[.18em] text-accent">Request sent</p><h1 className="mt-4 font-display text-5xl">The artist has your brief.</h1><p className="mt-4 text-muted">You’ll receive a message when they review the scope, timing, and material possibilities.</p><Button asChild className="mt-7"><Link href="/messages">View messages</Link></Button></div></main>; return <main className="mx-auto max-w-3xl px-5 py-14"><Link href="/explore" className="text-sm text-muted">← Explore Studios</Link><p className="mt-10 text-xs uppercase tracking-[.18em] text-accent">Custom creation</p><h1 className="mt-3 font-display text-5xl">Begin with an idea.</h1><form onSubmit={event => { event.preventDefault(); submitRequest({ studio: "Mira Kato", ...form }); setSent(true); }} className="mt-10 space-y-5 rounded-3xl border border-line p-6"><label className="block text-sm">What would you like made?<textarea required value={form.description} onChange={event => setForm({ ...form, description: event.target.value })} className="mt-2 min-h-32 w-full rounded-xl border border-line bg-transparent p-3" placeholder="Describe the object, feeling, use, or occasion…" /></label><label className="block text-sm">Reference images<input type="file" multiple className="mt-2 block w-full text-sm" /></label><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm">Budget<input required value={form.budget} onChange={event => setForm({ ...form, budget: event.target.value })} className="mt-2 h-11 w-full rounded-xl border border-line bg-transparent px-3" placeholder="$500–800" /></label><label className="text-sm">Ideal deadline<input required type="date" value={form.deadline} onChange={event => setForm({ ...form, deadline: event.target.value })} className="mt-2 h-11 w-full rounded-xl border border-line bg-transparent px-3" /></label></div><label className="block text-sm">Customization notes<textarea value={form.notes} onChange={event => setForm({ ...form, notes: event.target.value })} className="mt-2 min-h-24 w-full rounded-xl border border-line bg-transparent p-3" placeholder="Names, colours, materials, context…" /></label><Button type="submit">Send request to the artist</Button></form></main>; }
