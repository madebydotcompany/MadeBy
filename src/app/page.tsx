"use client";
import Image from "next/image";
import Link from "next/link";
import { motion } from "framer-motion";
import { Navigation } from "@/components/layout/navigation";
import { Footer } from "@/components/layout/footer";
import { Button } from "@/components/ui/button";
import { StudioCard } from "@/components/discovery/studio-card";
import { categories, stories, studios } from "@/features/discovery/data";
const slug = (value: string) => value.toLowerCase().replaceAll(/[^a-z0-9]+/g, "-").replace(/(^-|-$)/g, "");
export default function Home() { return <><Navigation /><main><section className="relative overflow-hidden"><div className="mx-auto grid min-h-[650px] max-w-7xl items-center gap-12 px-5 py-20 lg:grid-cols-[1.05fr_.95fr] lg:px-8 lg:py-24"><motion.div initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .7 }}><p className="mb-5 text-xs font-semibold uppercase tracking-[.2em] text-accent">A new kind of creative home</p><h1 className="max-w-3xl font-display text-5xl leading-[.96] tracking-tight sm:text-6xl lg:text-7xl">Every creator deserves a studio worth getting lost in.</h1><p className="mt-7 max-w-xl text-lg leading-8 text-muted">Enter the personal worlds of independent artists—where the story comes before the sale.</p><div className="mt-9 flex flex-wrap gap-3"><Button asChild size="lg"><Link href="/explore">Explore Studios <span aria-hidden>↗</span></Link></Button><Button asChild variant="outline" size="lg"><Link href="/studio-os">Build your Studio</Link></Button></div></motion.div><motion.div initial={{ opacity: 0, scale: .96 }} animate={{ opacity: 1, scale: 1 }} transition={{ duration: .8, delay: .15 }} className="relative mx-auto w-full max-w-lg"><Link href="/studios/mira-kato" aria-label="Visit Mira Kato Studio" className="block"><div className="absolute -inset-8 -z-10 rounded-full bg-accent/20 blur-3xl" /><div className="relative aspect-[4/5] overflow-hidden rounded-4xl shadow-soft"><Image src="https://images.unsplash.com/photo-1577083552431-6e5fd01988f7?auto=format&fit=crop&w=1200&q=90" alt="Sculptural ceramic vessel by Mira Kato" fill priority sizes="(max-width: 1024px) 90vw, 42vw" className="object-cover" /><div className="absolute inset-x-5 bottom-5 rounded-2xl border border-white/25 bg-black/20 p-4 text-white backdrop-blur-md"><p className="text-xs uppercase tracking-[.15em] text-white/70">Inside Mira's studio</p><p className="mt-1 font-display text-xl">Objects with a memory</p></div></div></Link></motion.div></div></section><section className="border-y border-line bg-ink/[.025]"><div className="mx-auto max-w-7xl px-5 py-7 lg:px-8"><p className="mb-5 text-center text-xs font-semibold uppercase tracking-[.18em] text-muted">Find a world to step into</p><div className="flex flex-wrap justify-center gap-x-8 gap-y-3">{categories.map(item => <Link href={`/categories/${slug(item.name)}`} className="text-sm transition hover:text-accent" key={item.name}>{item.name}</Link>)}</div></div></section><section className="mx-auto max-w-7xl px-5 py-24 lg:px-8"><Head eyebrow="Trending now" title="Studios people keep returning to." href="/explore" /><div className="mt-12 grid gap-6 md:grid-cols-3">{studios.slice(0, 3).map((studio, index) => <StudioCard key={studio.name} studio={studio} index={index} />)}</div></section><section className="bg-[#233d38] py-24 text-[#f6f3ea]"><div className="mx-auto max-w-7xl px-5 lg:px-8"><p className="text-xs font-semibold uppercase tracking-[.18em] text-[#e6c994]">Featured collection</p><h2 className="mt-5 max-w-xl font-display text-5xl leading-none sm:text-6xl">The soft geometry of home.</h2><p className="mt-6 max-w-md leading-7 text-[#f6f3ea]/70">Nine makers interpret the familiar object: a chair, a vessel, a lamp, a room.</p><Button asChild variant="outline" className="mt-8 border-[#f6f3ea]/30 text-[#f6f3ea] hover:bg-white/10"><Link href="/studios/mira-kato">Enter the collection</Link></Button></div></section><section className="mx-auto max-w-7xl px-5 py-24 lg:px-8"><Head eyebrow="Artist stories" title="The work behind the work." href="/journals/notes-from-a-quieter-kiln" /><div className="mt-12 grid gap-5 lg:grid-cols-3">{stories.map(story => <Link href={`/journals/${slug(story.title)}`} key={story.title} className="group overflow-hidden rounded-3xl bg-canvas shadow-card"><div className="relative aspect-[1.5]"><Image alt={story.title} src={story.image} fill className="object-cover transition duration-700 group-hover:scale-105" /></div><div className="p-6"><p className="text-xs text-muted">By {story.artist} · {story.read}</p><h3 className="mt-3 font-display text-3xl leading-tight">{story.title}</h3><p className="mt-4 text-sm text-accent">Read story →</p></div></Link>)}</div></section><section className="mx-auto max-w-7xl px-5 pb-24 lg:px-8"><Head eyebrow="Recently opened" title="New studios, first chapters." href="/explore" /><div className="mt-12 grid gap-6 md:grid-cols-3">{studios.slice(3).map((studio, index) => <StudioCard key={studio.name} studio={studio} index={index} />)}</div></section></main><Footer /></>; }
function Head({ eyebrow, title, href }: { eyebrow: string; title: string; href: string }) { return <div className="flex items-end justify-between gap-5"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-accent">{eyebrow}</p><h2 className="mt-3 font-display text-4xl sm:text-5xl">{title}</h2></div><Link href={href} className="hidden text-sm underline underline-offset-4 sm:block">View all →</Link></div>; }
